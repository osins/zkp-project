# 🚨 测试规范标准（项目级强制规则）

**版本：** v1.0.0  
**创建日期：** 2025-11-08  
**适用范围：** 所有测试代码（JavaScript/TypeScript/Rust/Python/等）  
**强制等级：** 最高优先级  
**违规后果：** CI/CD 自动拒绝 + 代码审查不通过

---

## 🎯 核心原则

> **"测试失败优于测试跳过"**  
> **"问题必须显现，不得掩盖"**  
> **"跳过测试就是欺骗用户"**

---

## 🚫 核心铁律：严禁忽略任何测试

### 规则 1：禁止使用任何形式的测试忽略语法

#### JavaScript/TypeScript（Jest/Vitest/Mocha）

```javascript
// ❌ 绝对禁止
describe.skip('测试套件', () => { ... });
test.skip('测试用例', () => { ... });
it.skip('测试用例', () => { ... });
xit('测试用例', () => { ... });
xdescribe('测试套件', () => { ... });
test.todo('待实现功能');
it.todo('待实现功能');

// ❌ 绝对禁止在代码中注释掉测试
/*
test('被注释的测试', () => {
    // ...
});
*/

// ❌ 绝对禁止条件跳过（除非环境限制，如 CI/本地）
if (process.env.SKIP_SLOW_TESTS) {
    test.skip('慢速测试', () => { ... });
}
```

#### Rust

```rust
// ❌ 绝对禁止
#[ignore]
#[test]
fn test_something() {
    // ...
}

// ❌ 绝对禁止注释测试
/*
#[test]
fn test_feature() {
    // ...
}
*/
```

#### Python (pytest/unittest)

```python
# ❌ 绝对禁止
@unittest.skip("reason")
def test_feature(self):
    pass

@pytest.mark.skip(reason="reason")
def test_feature():
    pass

# ❌ 绝对禁止注释测试
# def test_something():
#     pass
```

---

### 规则 2：测试失败必须显现，不得掩盖

#### 允许的行为：

- ✅ **让测试失败**：暴露问题是第一步
- ✅ **分析失败原因**：深入调查根本原因
- ✅ **报告问题**：生成详细的失败报告
- ✅ **修复问题**：在用户确认后修复
- ✅ **验证修复**：确保修复有效且无副作用

#### 禁止的行为：

- ❌ **跳过测试**：使用 `.skip`、`.todo`、`#[ignore]` 等
- ❌ **修改测试逻辑**：为了通过而降低测试标准
- ❌ **注释测试**：临时或永久注释掉失败的测试
- ❌ **假阳性**：修改测试使其假通过
- ❌ **隐藏错误**：捕获异常但不处理

---

### 规则 3：AI 测试失败处理流程（强制执行）

当 AI 遇到测试失败时，必须严格按照以下 7 步流程操作：

#### 步骤 1：立即停止所有操作

```
🛑 停止条件：
- 检测到任何测试失败
- 检测到测试超时
- 检测到测试错误（非断言失败）

🛑 禁止行为：
- 继续编写新代码
- 跳过失败的测试
- 自行决定修复方式
```

---

#### 步骤 2：深入分析问题

**必须收集的信息：**

1. **完整错误堆栈**：包括所有相关的堆栈帧
2. **根本原因**：不是表面现象，而是真正的原因
3. **影响范围**：
   - 受影响的功能模块
   - 受影响的其他测试
   - 潜在的连锁反应
4. **相关上下文**：
   - 最近的代码变更
   - 相关依赖的版本
   - 环境配置

---

#### 步骤 3：生成详细报告（使用标准模板）

**必须使用以下模板：**

```markdown
## 🚨 测试失败报告

**报告时间：** [时间戳]  
**报告人：** AI 助手  
**严重程度：** [低/中/高/紧急]

---

### 1. 失败的测试

- **文件路径：** `path/to/test/file.test.ts`
- **测试套件：** `describe('套件名')`
- **测试用例：** `test('用例名')`
- **失败时间：** [首次失败时间]

---

### 2. 错误信息（完整堆栈）

```
[粘贴完整的错误堆栈信息]
[包括所有相关的堆栈帧]
[不要截断或省略]
```

---

### 3. 根本原因分析

**问题本质：**
[详细描述根本原因，不是表面现象]

**为什么会发生：**
[解释导致问题的具体原因]

**相关代码：**
```language
[粘贴相关的代码片段]
```

---

### 4. 影响范围

#### 受影响的功能模块：
- [ ] 模块 A：[具体影响]
- [ ] 模块 B：[具体影响]

#### 受影响的其他测试：
- [ ] 测试 1：[状态：失败/可能受影响]
- [ ] 测试 2：[状态：失败/可能受影响]

#### 潜在的连锁反应：
- [描述可能的连锁问题]

#### 严重程度评估：
- **紧急程度：** [低/中/高/紧急]
- **影响范围：** [局部/模块级/系统级]
- **用户影响：** [无/低/中/高]

---

### 5. 解决方案（至少 3 个）

#### ✅ 方案 A：[简短描述，10 字内]

**详细描述：**
[完整的方案说明]

**优点：**
- ✅ [优点 1]
- ✅ [优点 2]

**缺点：**
- ⚠️ [缺点 1]
- ⚠️ [缺点 2]

**工作量：**
- 预计时间：[X 小时/天]
- 涉及文件：[N 个文件]
- 代码行数：[约 X 行]

**风险等级：** 🟢 低 / 🟡 中 / 🔴 高

**实施步骤：**
1. [步骤 1]
2. [步骤 2]
3. [步骤 3]

---

#### ✅ 方案 B：[简短描述，10 字内]

[同方案 A 的结构]

---

#### ✅ 方案 C：[简短描述，10 字内]

[同方案 A 的结构]

---

### 6. 推荐方案

**推荐：** 方案 [A/B/C]

**推荐理由：**
1. [理由 1]
2. [理由 2]
3. [理由 3]

**权衡说明：**
[解释为什么选择这个方案，以及权衡了哪些因素]

---

### 7. 需要您的决策

⚠️ **在您确认之前，我不会进行任何修复操作。**

**请选择：**
- [ ] 方案 A：[简短描述]
- [ ] 方案 B：[简短描述]
- [ ] 方案 C：[简短描述]
- [ ] 其他建议：___________

**或者：**
- [ ] 需要更多信息（请说明）
- [ ] 暂不修复（请说明原因）

---

### 附录

**相关文件：**
- [文件 1]
- [文件 2]

**相关文档：**
- [文档 1]
- [文档 2]

**参考资料：**
- [链接 1]
- [链接 2]
```

---

#### 步骤 4：等待用户确认（关键步骤）

**关键原则：**
> **"AI 负责分析和建议，用户负责决策"**

**等待确认期间：**
- ✅ 可以：回答用户的澄清问题
- ✅ 可以：提供更多技术细节
- ✅ 可以：补充分析遗漏的方面
- ❌ 禁止：自行开始修复
- ❌ 禁止：修改测试代码
- ❌ 禁止：跳过测试

**用户可能的回应：**
1. 选择某个方案 → 执行步骤 5
2. 提出新方案 → 分析可行性，更新报告
3. 要求更多信息 → 补充分析
4. 暂不修复 → 记录决策，停止流程

---

#### 步骤 5：执行用户选择的方案

**执行原则：**
- ✅ 严格按照用户确认的方案执行
- ✅ 不得擅自修改方案细节
- ✅ 如需调整，必须再次征求用户同意

**执行过程：**
1. 修改代码
2. 增量验证（每改一处就验证）
3. 记录所有变更

---

#### 步骤 6：验证修复结果

**验证清单：**

```
☐ 失败的测试现已通过
☐ 所有相关测试仍然通过
☐ 没有引入新的测试失败
☐ 没有引入新的 linter 错误
☐ 代码覆盖率没有下降
☐ 功能行为符合预期
```

**验证命令：**
```bash
# 运行失败的测试
npm test -- path/to/test.test.ts

# 运行整个测试套件
npm test

# 检查代码覆盖率
npm run test:coverage
```

---

#### 步骤 7：报告修复结果

**使用以下模板：**

```markdown
## ✅ 修复完成报告

**修复时间：** [时间戳]  
**执行方案：** 方案 [A/B/C]

---

### 1. 执行的方案

**方案名称：** [方案 X]

**实际执行：**
[描述实际执行过程，是否与计划一致]

---

### 2. 修改的文件

- ✏️ `path/to/file1.ts` - [修改说明]
- ✏️ `path/to/file2.ts` - [修改说明]
- ➕ `path/to/file3.ts` - [新增说明]
- ➖ `path/to/file4.ts` - [删除说明]

---

### 3. 测试结果

- ✅ 失败测试现已通过
- ✅ 所有相关测试通过（[N/N]）
- ✅ 无新问题引入
- ✅ 代码覆盖率：[X%]（[上升/下降/持平]）

**测试输出：**
```
[粘贴测试运行输出]
```

---

### 4. 验证命令

用户可以执行以下命令验证修复：

```bash
# 验证失败的测试
npm test -- path/to/test.test.ts

# 运行完整测试套件
npm test

# 检查覆盖率
npm run test:coverage
```

---

### 5. 后续建议（可选）

[如有必要，提供后续优化建议]

---

### 6. 总结

✅ **修复状态：** 成功  
✅ **测试状态：** 全部通过  
✅ **质量检查：** 通过  

**问题已解决，可以继续开发。**
```

---

## 📊 执行检查清单（AI 必须遵守）

在处理测试问题时，AI 必须逐项确认：

```
☐ 1. 是否遇到测试失败？
  ├─ 是 → 继续下一步
  └─ 否 → 正常流程

☐ 2. 是否已立即停止所有操作？
  ├─ 是 → 继续下一步
  └─ 否 → 🚨 立即停止！

☐ 3. 是否已深入分析根本原因？
  ├─ 是 → 继续下一步
  └─ 否 → 🚨 深入分析，不要表面分析！

☐ 4. 是否已生成详细报告？
  ├─ 是 → 继续下一步
  └─ 否 → 🚨 使用标准模板生成报告！

☐ 5. 是否已提供至少 3 个解决方案？
  ├─ 是 → 继续下一步
  └─ 否 → 🚨 补充方案至至少 3 个！

☐ 6. 是否每个方案都包含完整信息？
     (优缺点、工作量、风险、步骤)
  ├─ 是 → 继续下一步
  └─ 否 → 🚨 补充完整信息！

☐ 7. 是否已明确推荐某个方案？
  ├─ 是 → 继续下一步
  └─ 否 → 🚨 给出推荐并说明理由！

☐ 8. 是否已获得用户明确确认？
  ├─ 是 → 执行修复（步骤 5）
  └─ 否 → 🚨 等待确认，禁止自行修复！

☐ 9. 是否严格按照用户确认的方案执行？
  ├─ 是 → 继续验证
  └─ 否 → 🚨 停止！重新征求用户同意！

☐ 10. 是否已验证修复结果？
   ├─ 是 → 生成修复报告
   └─ 否 → 🚨 必须验证所有测试！

☐ 11. 是否已生成修复完成报告？
   ├─ 是 → ✅ 流程完成
   └─ 否 → 🚨 使用标准模板生成报告！
```

---

## 🔍 常见违规行为示例（禁止）

### ❌ 违规示例 1：直接跳过测试

```javascript
// ❌ 禁止
describe.skip('Halo2 电路测试', () => {
    test('应该生成证明', () => {
        // ...
    });
});
```

**为什么禁止：**
- 掩盖了问题
- 用户看不到失败
- 问题会在生产环境爆发

**正确做法：**
1. 让测试失败
2. 生成失败报告
3. 提供解决方案
4. 等待用户确认
5. 修复问题

---

### ❌ 违规示例 2：自行修复问题

```javascript
// AI 发现测试失败后
// ❌ 禁止：直接修复
const fixed = await fixBuildTarget(); // 自行修复

// ✅ 正确：报告问题
// 1. 停止操作
// 2. 生成报告
// 3. 等待用户确认
// 4. 用户确认后才修复
```

---

### ❌ 违规示例 3：修改测试逻辑

```javascript
// 原始测试（失败）
test('应该返回 42', () => {
    expect(calculate()).toBe(42);
});

// ❌ 禁止：修改测试使其通过
test('应该返回 42', () => {
    expect(calculate()).toBe(41); // 改成实际返回值
});

// ✅ 正确：修复实现代码
function calculate() {
    return 42; // 修复实现，而不是测试
}
```

---

### ❌ 违规示例 4：批量修复不报告

```javascript
// ❌ 禁止：同时修复多个问题而不报告
async function fixAllTests() {
    await fixTest1(); // 修复测试 1
    await fixTest2(); // 修复测试 2
    await fixTest3(); // 修复测试 3
    // 没有报告，没有等待确认
}

// ✅ 正确：逐个报告并确认
// 1. 报告测试 1 的问题
// 2. 等待用户确认
// 3. 修复测试 1
// 4. 验证测试 1
// 5. 重复 1-4 处理测试 2、3
```

---

## 🛡️ 自动化强制执行

### Git Pre-commit Hook

在 `.git/hooks/pre-commit` 中添加：

```bash
#!/bin/bash

echo "🔍 检查测试跳过语法..."

# 检查 JavaScript/TypeScript
if git diff --cached --name-only | grep -E '\.(test|spec)\.(js|ts|jsx|tsx)$' > /dev/null; then
    if git diff --cached --diff-filter=ACM -U0 | grep -E '\.(skip|todo|xit|xdescribe)' > /dev/null; then
        echo "❌ 错误：发现测试跳过语法！"
        echo "禁止使用 .skip, .todo, xit, xdescribe"
        echo "请让测试失败，并报告问题。"
        exit 1
    fi
fi

# 检查 Rust
if git diff --cached --name-only | grep '\.rs$' > /dev/null; then
    if git diff --cached --diff-filter=ACM -U0 | grep '#\[ignore\]' > /dev/null; then
        echo "❌ 错误：发现 #[ignore] 注解！"
        echo "禁止忽略测试。"
        exit 1
    fi
fi

echo "✅ 测试规范检查通过"
```

### CI/CD 检查

在 CI/CD 配置中添加：

```yaml
# .github/workflows/test.yml
name: Test Standards Check

on: [push, pull_request]

jobs:
  check-test-standards:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Check for test skips
        run: |
          if grep -r "\.skip\|\.todo\|xit\|xdescribe" **/*.test.{js,ts,jsx,tsx}; then
            echo "❌ 发现测试跳过语法！"
            exit 1
          fi
          
          if grep -r "#\[ignore\]" **/*.rs; then
            echo "❌ 发现 Rust 测试忽略！"
            exit 1
          fi
          
          echo "✅ 测试规范检查通过"
```

---

## 📝 总结

### 核心原则（4 条铁律）

1. **严禁跳过测试**：任何形式的 `.skip`、`.todo`、`#[ignore]`
2. **问题必须显现**：测试失败优于测试跳过
3. **AI 分析报告，用户决策执行**：AI 不得自行修复
4. **修复前确认，修复后验证**：确保质量和透明度

### AI 工作流程（7 步法）

```
测试失败
    ↓
1. 立即停止所有操作
    ↓
2. 深入分析问题
    ↓
3. 生成详细报告（标准模板）
    ↓
4. 等待用户确认 ← 关键步骤！
    ↓
5. 执行用户选择的方案
    ↓
6. 验证修复结果
    ↓
7. 报告修复结果
```

### 一句话精髓

> **"测试失败是信号，不是噪音；跳过测试就是欺骗，问题必须显现。"**

---

## 📚 附录

### A. 相关文档

- [AI 编程 7 铁律](./AI_CODING_7_RULES.md)
- [代码质量标准](./CODE_QUALITY_STANDARDS.md)
- [CI/CD 规范](./CI_CD_STANDARDS.md)

### B. 常见问题

**Q1: 如果测试在某些环境下无法运行怎么办？**

A1: 使用环境条件判断，但不能跳过：

```javascript
// ✅ 允许
test('功能测试', () => {
    if (!process.env.CI) {
        // 本地环境跳过
        return;
    }
    // CI 环境运行
});

// ❌ 禁止
test.skip('功能测试', () => { ... });
```

**Q2: 如果需要临时禁用某个测试怎么办？**

A2: 不存在"临时"禁用。必须：
1. 让测试失败（显现问题）
2. 创建 Issue 跟踪
3. 尽快修复

**Q3: 如果测试依赖外部服务怎么办？**

A3: 使用 Mock，但测试不能跳过：

```javascript
// ✅ 正确
test('API 调用', async () => {
    const mockApi = jest.fn().mockResolvedValue({ data: 'test' });
    // 测试 mockApi
});
```

---

**版本历史：**
- v1.0.0 (2025-11-08): 初始版本

**维护者：** 项目团队  
**最后更新：** 2025-11-08
